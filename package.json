{
  "name": "infinite-screen",
  "version": "1.0.0",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "prebuild": "rm -rf dist/",
    "build": "npm run prebuild && parcel build src/index.html",
    "dev": "parcel src/index.html",
    "deploy": "gh-pages -d dist/"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "konva": "^8.1.3",
    "parcel-bundler": "^1.12.5",
    "query-string": "^7.0.1"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "2.0.0-beta.2"
  }
}
